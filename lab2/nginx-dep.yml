apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-pv
          mountPath: /etc/nginx
          subPath: nginx
        - name: nginx-pv
          mountPath: /usr/share/nginx/html
          subPath: html

      volumes:
      - name: nginx-pv
        persistentVolumeClaim:
          claimName: nginx-pvc
